name: Delete

on:
  workflow_dispatch:
  schedule:
    - cron: "0 9 * * 0"

jobs:
  delete:
    name: Delete
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: read

    steps:
      - name: Delete workflow runs
        uses: Mattraks/delete-workflow-runs@v2
        with:
          retain_days: 0
          keep_minimum_runs: 0
